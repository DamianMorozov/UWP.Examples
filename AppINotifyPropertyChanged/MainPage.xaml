<Page
    x:Class="AppINotifyPropertyChanged.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AppINotifyPropertyChanged"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodel="using:AppINotifyPropertyChanged.ViewModel"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    FontSize="22" Name="pageMain" 
    d:DesignHeight="320" d:DesignWidth="320">

    <Page.Resources>
        <viewmodel:FontSettings x:Key="MyFontSettings"></viewmodel:FontSettings>
    </Page.Resources>

    <Grid>
        <Grid.DataContext>
            <viewmodel:FontSettings/>
        </Grid.DataContext>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <TextBlock Name="textBlock" Grid.Row="0" Text="Font size" FontSize="22"
            VerticalAlignment="Center" HorizontalAlignment="Center"></TextBlock>
        <!--Text="{Binding Size, Mode=TwoWay, Source={StaticResource MyFontSettings}, UpdateSourceTrigger=PropertyChanged}"-->
        <TextBox Name="textBox" Grid.Row="1" Grid.Column="1" 
            Text="{Binding Size, Mode=TwoWay, Source={StaticResource MyFontSettings}, UpdateSourceTrigger=PropertyChanged}"
            VerticalAlignment="Center" HorizontalAlignment="Center"
            FontSize="22" TextChanged="textBox_TextChanged"
            ></TextBox>
    </Grid>
</Page>
